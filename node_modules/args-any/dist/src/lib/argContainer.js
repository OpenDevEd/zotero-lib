"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.argContainer = void 0;
exports.argContainer = (args, map) => {
    const all = () => args;
    const options = () => {
        const result = [];
        for (let i = 0; i < args.length; i += 1) {
            const key = args[i];
            if (map.has(key)) {
                result.push(args[i]);
                const option = map.get(key);
                if (option && option.value) {
                    result.push(option.value);
                    i += 1;
                }
            }
        }
        return result;
    };
    const other = () => {
        const optionArgs = options();
        return all().filter(x => !optionArgs.includes(x));
    };
    const container = {
        all,
        options,
        other
    };
    return container;
};
//# sourceMappingURL=argContainer.js.map