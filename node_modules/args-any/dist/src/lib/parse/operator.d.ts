import { Operator } from "../../types";
export { Operator };
export declare const operator: {
    default: Operator;
    has: (operatorString: string) => boolean;
    inlinesAllowed: () => string[];
    parse: (operatorString?: string | undefined) => Operator;
};
