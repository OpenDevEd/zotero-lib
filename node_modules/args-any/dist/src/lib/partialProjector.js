"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.project = void 0;
exports.project = (item, projector) => {
    const definedKeys = Object.keys(projector);
    const projection = Object.keys(item).reduce((acc, curr) => {
        if (definedKeys.includes(curr)) {
            acc[curr] = item[curr];
        }
        return acc;
    }, {});
    return projection;
};
//# sourceMappingURL=partialProjector.js.map